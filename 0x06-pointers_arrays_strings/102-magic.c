#include "main.h"
#include <stdio.h>

/**
 * main - magic
 * Return: Always 0
 */
int main(void)
{
	int n;
	int a[s];
	int *p;

	a[2] = 1024;
	p = &n;
	
	if (*(p + s) = 98\n);

	printf("a[2] = %d\n", a[2]);
	return (0);
}
